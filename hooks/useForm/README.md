# useForm

Ejemplo:

```
    const initialForm = {
        name: 'Alejandro',
        age: 23,
        email:'email@gmail.com'
    }
    cons [formValues, handleInputChange, reset] = useForm(initialForm);
```